import 'package:flutter/material.dart';

/// A collection of color constants used throughout the app.
const recipeAppBackground = Color(0xfffdffff);

/// The color used for the app bar background.
const backgroundColor = Color(0xFFf7f8fb);

/// The color used for the app bar text.
const textGreen = Color(0xFF50bf6e);

/// The color used for the search bar background.
const searchBarColor = Color(0xfff6f7fb);

/// The color used for the search bar icon.
const gradientColor = LinearGradient(
  colors: [Colors.green, Colors.greenAccent],
);

/// The color used for the search bar text.
LinearGradient gradinet = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [Colors.grey.shade400, Colors.grey.shade200, Colors.white],
);
